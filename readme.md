## git empty

```
git init
git add *
git add .gitignore
git add .dockerignore
git add .github
git commit -am "first version"
git branch -M main
git remote add origin git@github.com:zzci/empty.git
git push -u origin main
```


